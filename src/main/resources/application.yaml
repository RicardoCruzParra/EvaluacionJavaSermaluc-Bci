server:
  port: 8080

spring:
  application:
    name: users-api

  datasource:
    url: jdbc:h2:mem:usersdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  h2:
    console:
      enabled: true
      path: /h2-console

springdoc:
  swagger-ui:
    path: /swagger-ui.html

security:
  password-regex: "^(?=.*[A-Z])(?=.*\\d).{8,}$"
  jwt:
    secret: "clave-muy-larga-de-al-menos-32-caracteres-1234567890"
    expiration-minutes: 60

logging:
  level:
    root: INFO
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
